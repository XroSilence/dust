services:
  - type: web
    name: dustup-quote-api
    env: node
    buildCommand: npm install
    startCommand: node server.cjs
    envVars:
      - key: EMAIL_USER
        value: services@dustup.online
      - key: EMAIL_PASS
      