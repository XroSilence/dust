services:
  - type: web
    name: dustup-quote-api
    runtime: node
    buildCommand: npm install
    startCommand: node ../dist/server.js
    envVars:
      - key: EMAIL_USER
      - key: EMAIL_PASS
        
      